import Swiper from 'swiper';
import { Navigation, Pagination, EffectFade, Parallax } from 'swiper/modules';

export default {
    Swiper,
    Navigation,
    Pagination,
    EffectFade,
    Parallax,
}